export const selectSavedMoviesById = (state) => state.savedMovies.byId;
export const selectSavedMoviesAllIds = (state) => state.savedMovies.allIds;
export const selectSearchConfig = (state) => state.searchConfig;
export const selectSearchTerm = (state) => state.searchTerm;
export const selectSearchedMovies = (state) => state.searchedMovies;
